# Building the Project
- **Runtime:** Node.js v20+ / PNPM.
- **Build:** `pnpm build` (outputs to `/dist`).
- **Env:** Use `.env.example` as the source of truth for environment variables.
- **Checklist:** Always run `pnpm lint` before committing.